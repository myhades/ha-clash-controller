[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    system: starts a real Clash-compatible core and loads the integration in Home Assistant
    release: slower fault, recovery, and lifecycle validation required before release
